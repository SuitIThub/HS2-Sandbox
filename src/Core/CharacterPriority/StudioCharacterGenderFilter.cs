namespace HS2SandboxPlugin
{
    /// <summary>Gender filter when resolving characters from the priority list.</summary>
    public enum StudioCharacterGenderFilter
    {
        Any = 0,
        Male = 1,
        Female = 2
    }
}
